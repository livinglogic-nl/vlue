module.exports = class SourceBundler {
    getMemory(name) {
    }

    addScript(name, str, reloadMethod) {
    }

    addStyle(name, str) {
    }

    addHtml(name, str) {
    }

    buildScript(sourceMap, rootRun) {
    }

    buildStyle() {
    }

}
